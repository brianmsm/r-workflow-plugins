interface:
  display_name: "Quarto Computation Performance"
  short_description: "Review expensive Quarto execution."
  default_prompt: "Use $quarto-computation-performance to review expensive renders, do-not-execute requests, cache/freeze, simulations, model fitting, generated artifacts, and proportional validation."
policy:
  allow_implicit_invocation: true
