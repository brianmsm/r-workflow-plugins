interface:
  display_name: "Quarto Authoring Core"
  short_description: "Review Quarto .qmd source mechanics."
  default_prompt: "Use $quarto-authoring-core to review Quarto .qmd Markdown, labels, citations, executable cells, and cross-references, and hand off narrative, format, render, or expensive-computation issues."

policy:
  allow_implicit_invocation: true
