interface:
  display_name: "Quarto Authoring Core"
  short_description: "Review Quarto .qmd authoring across prose, R, Python, and Julia."
  default_prompt: "Use $quarto-authoring-core to review and improve a Quarto .qmd document, including its Markdown, YAML, executable cells, and cross-references."

policy:
  allow_implicit_invocation: true
