interface:
  display_name: "Quarto Render Troubleshooting"
  short_description: "Verify Quarto renders and artifacts."
  default_prompt: "Use $quarto-render-troubleshooting to diagnose render failures or verify rendered artifacts, including DOCX/PDF/HTML checks, links, citations, screenshots, and post-render output."
policy:
  allow_implicit_invocation: true
