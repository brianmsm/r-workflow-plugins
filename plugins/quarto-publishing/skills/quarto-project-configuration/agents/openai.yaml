interface:
  display_name: "Quarto Project Configuration"
  short_description: "Review shared Quarto project YAML."
  default_prompt: "Use $quarto-project-configuration to review _quarto.yml, _metadata.yml, project structure, profiles, render targets, and document-vs-project YAML placement."

policy:
  allow_implicit_invocation: true
